project.ext {
    supportVersion = '19.1.+'
    playServicesVersion = '4.4.+'
    asmVersion = '5.0.2'
}

allprojects {
    buildscript {
        repositories {
            mavenCentral()
        }
    }

    dependencies {
        repositories {
            mavenCentral()
            mavenLocal()
        }
    }

//    group = GROUP
//    version = VERSION_NAME

    apply plugin: 'maven'
}

task wrapper(type: Wrapper) {
    gradleVersion = '1.10'
}

task cleanExample(type: Exec) {
    executable = 'gradle'
    workingDir = project.file('example')
    args = [ 'clean' ]
}

task assembleExample(type: Exec) {
    executable = 'gradle'
    workingDir = project.file('example')
    args = [ 'assemble' ]
}

task installExample(type: Exec) {
    executable = 'gradle'
    workingDir = project.file('example')
    args = [ 'installDebug' ]
}

task cleanTests09(type: Exec) {
    executable = 'gradle'
    workingDir = project.file('tests09')
    args = [ 'clean' ]
}

task assembleTests09(type: Exec) {
    executable = 'gradle'
    workingDir = project.file('tests09')
    args = [ 'assemble' ]
}

task runTests09(type: Exec) {
    executable = 'gradle'
    workingDir = project.file('tests09')
    args = [ 'connectedAndroidTest' ]
}

task cleanTests010(type: Exec) {
    executable = 'gradle'
    workingDir = project.file('tests010')
    args = [ 'clean' ]
}

task assembleTests010(type: Exec) {
    executable = 'gradle'
    workingDir = project.file('tests010')
    args = [ 'assemble' ]
}

task runTests010(type: Exec) {
    executable = 'gradle'
    workingDir = project.file('tests010')
    args = [ 'connectedAndroidTest' ]
}
